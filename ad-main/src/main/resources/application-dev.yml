#custom
custom:
  mysql:
    address: ecs.izhaoo.com
    password: fkz
    port: 3306
    database: fkz
    username: fkz
    config: autoReconnect=true&useSSL=false&useUnicode=true&characterEncoding=utf8&useAffectedRows=true

#用于显示绑定的参数具体值

logging:
  level:
    org:
      hibernate:
        #        SQL: debug
        type:
          descriptor:
            sql:
              BasicBinder: trace